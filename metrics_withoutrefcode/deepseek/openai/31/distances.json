[
    {
        "distance": 0.3865636792079273,
        "from_label": "import numpy as npimport pandas as pdimport matplotlib.pyplot as pltimport seaborn as snsx = np.linspace(0, 2 * np.pi, 400)y1 = np.sin(x)y2 = np.cos(x)# plot x vs y1 and x vs y2 in two subplots, sharing the x axis# SOLUTION START",
        "to_label": "Import the necessary libraries: numpy for numerical operations, pandas for data manipulation (though not directly used here), matplotlib.pyplot for plotting, and seaborn for enhanced visualizations."
    },
    {
        "distance": 0.5973129010569963,
        "from_label": "Import the necessary libraries: numpy for numerical operations, pandas for data manipulation (though not directly used here), matplotlib.pyplot for plotting, and seaborn for enhanced visualizations.",
        "to_label": "Generate the x values using numpy's linspace function, which creates 400 evenly spaced points between 0 and 2π."
    },
    {
        "distance": 0.28035938994277815,
        "from_label": "Generate the x values using numpy's linspace function, which creates 400 evenly spaced points between 0 and 2π.",
        "to_label": "Compute the y values for the sine and cosine functions using numpy's sin and cos functions, respectively."
    },
    {
        "distance": 1.0,
        "from_label": "Compute the y values for the sine and cosine functions using numpy's sin and cos functions, respectively.",
        "to_label": "Create a figure with two subplots that share the x-axis. This can be done using plt.subplots with the parameters nrows=2 (for two rows of subplots) and sharex=True (to share the x-axis)."
    },
    {
        "distance": 0.4630192952484048,
        "from_label": "Create a figure with two subplots that share the x-axis. This can be done using plt.subplots with the parameters nrows=2 (for two rows of subplots) and sharex=True (to share the x-axis).",
        "to_label": "Plot the sine function (y1) in the first subplot (ax[0]) and the cosine function (y2) in the second subplot (ax[1]). Use the plot method of the axes objects for this."
    },
    {
        "distance": 0.29550083618113726,
        "from_label": "Plot the sine function (y1) in the first subplot (ax[0]) and the cosine function (y2) in the second subplot (ax[1]). Use the plot method of the axes objects for this.",
        "to_label": "Customize the plots by adding titles and labels. For example, set the title of the first subplot to 'Sine Function' and the second to 'Cosine Function'. Label the x-axis as 'x' and the y-axis as 'y'."
    },
    {
        "distance": 0.7575706207129331,
        "from_label": "Customize the plots by adding titles and labels. For example, set the title of the first subplot to 'Sine Function' and the second to 'Cosine Function'. Label the x-axis as 'x' and the y-axis as 'y'.",
        "to_label": "Display the plots using plt.show(). This will render the figure with the two subplots."
    },
    {
        "distance": 0.4084244088974916,
        "from_label": "Display the plots using plt.show(). This will render the figure with the two subplots.",
        "to_label": "fig, (ax1, ax2) = plt.subplots(nrows=2, sharex=True)plt.subplots_adjust(hspace=0.0)ax1.grid()ax2.grid()ax1.plot(x, y1, color=\"r\")ax2.plot(x, y2, color=\"b\", linestyle=\"--\")"
    }
]